import 'package:flutter/material.dart';

class TrackVoteScreen extends StatelessWidget {
  const TrackVoteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Track Vote')),
    );
  }
}
